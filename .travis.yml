language: php

# use the new container infrastructure
sudo: false

php:
  - "5.3"
  - "5.4"
  - "5.5"
  - "5.6"
  - '7.0'
  - "hhvm"

cache:
    directories:
        - vendor

before_script:
  - composer self-update
  - composer update --prefer-dist

script:
    - ./bin/phpspec run
